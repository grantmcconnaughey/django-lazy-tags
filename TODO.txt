Soon
* Make lazy_tag decorator work with inclusion_tag
* Don't hardcode lazy tag view URL in JavaScript
* Transparent AJAX spinner

Someday
* Work with a tag where takes_context = True
* Work with Django models, not just pks
* Send csrf_token